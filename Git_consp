Git --

git config -l
git config --global user.name ""
git config --global user.mail ""
git config --global core.edit "vim"


git diff --chaced 
git diff --staged 
			сравнить  что проиндексировал с master

git diff 
			сравнить не проиндексирован с masterr

git commit -a -m 'added new benchmarks' 
			игнор add .

git rm <name.file>
git rm -f <name.file> 
			удалить проиндексирован fail

git rm --cached
			удалить проиндексирован file из add

git mv <file.name> <new.fiele.name>

git log -p -2

git log --stat 
			кратко инфо

git log --graph 
			график

gti log -p
			выводит diff 
git log --stat 
			статистику
git log --since=2.weeks 
			2недели

	`--since`, `--after`
			Ограничить коммиты теми, которые сделаны после указанной даты.
	`--until`, `--before`
			Ограничить коммиты теми, которые сделаны до указанной даты.
	`--author`
			Показать только те коммиты, автор которых соответствует указанной строке.
	`--committer`
			Показать только те коммиты, коммитер которых соответствует указанной строке

git commit --amend
			изменить последний коммит

	$ git commit -m 'initial commit'
	$ git add forgotten_file
	$ git commit --amend

	